---
import Footer from '../components/Footer/Footer.astro'
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Iglesia Edén Terrassa - Una comunidad ministerial en constante crecimiento que colabora con la iglesia global." />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		
		<!-- Open Graph / Facebook -->
		<meta property="og:type" content="website" />
		<meta property="og:url" content={Astro.url} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content="Iglesia Edén Terrassa - Una comunidad ministerial en constante crecimiento que colabora con la iglesia global." />
		<meta property="og:image" content="/images/hero/hero-image.webp" />

		<!-- Twitter -->
		<meta property="twitter:card" content="summary_large_image" />
		<meta property="twitter:url" content={Astro.url} />
		<meta property="twitter:title" content={title} />
		<meta property="twitter:description" content="Iglesia Edén Terrassa - Una comunidad ministerial en constante crecimiento que colabora con la iglesia global." />
		<meta property="twitter:image" content="/images/hero/hero-image.webp" />

		<link rel="canonical" href={Astro.url} />
	</head>
	<body>
		<slot />
	</body>	
</html>
<style is:global>
	
	@font-face {
		font-family: 'Chalkboard';
		/* src: url('/fonts/chalkboard.ttf') format('truetype'); */
		font-weight: normal;
		font-style: normal;
	}

	* {
		box-sizing: border-box;
		margin: 0;
		padding: 0;
		/* overflow: hidden; */
	}
	html {
		font-family: system-ui, sans-serif;
		scroll-behavior: smooth;		
	}
	body {
		width: 100%;
		min-height: 100vh;

		background-color: #333;
		/* background-color: #ddd; */
		color: #f7f7f7f7;
	}

	.hr_custom {
		top: 0;
		height: 3px; /* Equivalente a h-[2px] en Tailwind */
		width: 90%; /* Equivalente a w-full en Tailwind */
		min-width: 18rem; /* Equivalente a min-w-[18rem] en Tailwind */
		border-top: 0; /* Equivalente a border-t-0 en Tailwind */
		background: transparent; /* Equivalente a bg-transparent en Tailwind */
		background-image: linear-gradient(to right, transparent, #78866b, transparent); /* Equivalente a bg-gradient-to-r from-transparent via-white to-transparent en Tailwind */
		background-position: center;
	}

	h2 {
		/* font-family: 'chalkboard'; */
		font-size: 40px;
		
	}
</style>
